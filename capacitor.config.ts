import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lcgeletro.calculadorashopee',
  appName: 'Calculadora Shopee',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
